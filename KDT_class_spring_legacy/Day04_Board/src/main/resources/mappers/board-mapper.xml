<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">
    <select id="getPostList" resultType="com.kedu.dto.BoardDTO">
        SELECT * FROM (SELECT board.*, ROW_NUMBER() OVER(ORDER BY id DESC) rn FROM board
        <if test="search!=null">
            WHERE ${searchOpt} like '%'||#{search}||'%'
        </if>
        ORDER BY id DESC) WHERE rn BETWEEN #{start} AND #{end}
    </select>
    <select id="selectById" resultType="com.kedu.dto.BoardDTO">
        SELECT * FROM board WHERE id=#{target}
    </select>
    <select id="getMaxPage" resultType="Integer">
        SELECT count(*) FROM board
        <if test="search!=null">
            WHERE ${searchOpt} like '%'||#{search}||'%'
        </if>
    </select>

    <insert id="insert">
        INSERT INTO board(id, writer, title, contents) VALUES(board_seq.nextval, #{writer}, #{title}, #{contents})
        <selectKey order="AFTER" keyProperty="id" resultType="Integer">
            SELECT board_seq.currval from dual
        </selectKey>
    </insert>

    <delete id="delete">
        DELETE FROM board WHERE id=#{target}
    </delete>

    <update id="updatePost">
        UPDATE board SET title=#{title}, contents=#{contents} WHERE id=#{id}
    </update>
    <update id="updateViewCount">
        UPDATE board SET view_count = view_count+1 WHERE id=#{target}
    </update>
</mapper>