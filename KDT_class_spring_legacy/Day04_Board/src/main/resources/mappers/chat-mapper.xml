<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Chat">
    <select id="getChatHistory" resultType="com.kedu.dto.ChatDTO">
        SELECT * FROM chat ORDER BY send_date ASC
    </select>

    <insert id="insert">
        INSERT INTO chat(seq, sender, message, send_date) VALUES(chat_seq.nextval, #{sender}, #{message}, #{sendDate})
    </insert>
</mapper>